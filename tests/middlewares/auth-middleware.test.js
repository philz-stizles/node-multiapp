// Jest provides the 'test function' as a global in these test suite files, thus you do not need to require it
const authMiddleware = require('./../../src/middlewares/auth-middleware')

test('Auth Middleware', () => {
    // const mockReq = {

    // }
    // const mockRes = {}

    // authMiddleware(mockReq, mockRes, () => {})
})